version: "3"

services:
  app:
    build:
      context: .
    environment:
      - AUTH0_ID=<AUTH0_ID>

  myproxy:
    image: nginx:alpine
    volumes:
    - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
    - 8080:80